import React from "react";
import Main from "./Main";
import Settings from "../settings/Settings";

const SettingsPage = () => {
  return <Main subpageContent={<Settings />} />;
};

export default SettingsPage;
